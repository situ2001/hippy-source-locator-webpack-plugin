# Changesets

Run `pnpm changeset` for every user-visible change. Select the appropriate semantic
version bump and describe the change in release-note language.

Maintainers can apply pending versions and update `CHANGELOG.md` with
`pnpm version-packages`, then publish the prepared release with `pnpm release`.
